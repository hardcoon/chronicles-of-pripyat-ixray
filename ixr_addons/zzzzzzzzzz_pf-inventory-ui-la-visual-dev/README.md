# Prototype 002 + Lost Alpha EE item grid (DEV)

Active base: saved prototype `002_right_window_soft_worn_stock_button`.

Added over 002:

- Lost Alpha EE item-cell grid;
- Lost Alpha EE olive hover/selected-cell treatment;
- left stash/search window in the same prototype 002 material;
- upper centre equipment area in prototype 002 graphite;
- five stock artefact cells retained at their original coordinates and restyled in Prototype 002 graphite;
- lower status module rebuilt with crisp orange-on-grey 10 px bars, percentages, hunger and thirst;
- exact LA EE Player parameters pictograms used for every status row;
- status tracks shifted right while the percentage columns stay fixed;
- lower status backing forced fully opaque to prevent world bleed-through;
- accumulated radiation/bleeding warning lamps intentionally omitted;
- PDA-related inventory action plaques for Take all / Put all;
- all action-button states derived from the user's approved enabled DDS;
- trade-only Buy/Sell controls and separator restyled without coordinate changes;
- repair/upgrade window widened to the full left column with an integrated mechanic card, item stand and upgrade bay;
- repair action uses the approved inventory button family and upgrade nodes keep distinct state colours;
- stock conversation frame and its action button restyled without changing dialogue coordinates;
- conversation transcript and reply wells use translucent inventory graphite while the action button is attached to the lower footer;
- conversation transcript and reply lists use their own slim graphite scrollbar with restrained amber accents;
- actor-menu item grids use a narrow amber inventory scrollbar profile; stock and PDA scrollbar profiles remain unchanged;
- only quit-to-menu and quit-to-desktop confirmation boxes restyled; all other message boxes stay stock;
- inventory item tooltip and context-menu surfaces harmonised while retaining all stock data widgets and actions;
- outfit tooltip uses the exact inventory status pictograms and the requested column-major protection order;
- TEST: reference-like brighter warm graphite applied only to existing background planes;
- TEST: background pass keeps all inventory coordinates unchanged;
- status sticks use their original 10 UI px height and percentages use an inventory-only 17 px font;
- no decorative strip below the item grid.

The original `gamedata` files are untouched. The addon preserves the stock IX-Ray XML hierarchy and the prototype 002 coordinates.
