; GP-34 is a distinct inventory section in STCoP. IX-Ray compares the
; configured launcher section literally, so the default GP-25 binding rejects
; GP-34 even though both rifles already expose an attachable launcher slot.

![wpn_abakan]
grenade_launcher_name = wpn_addon_grenade_launcher_gp34

![wpn_aek971]
grenade_launcher_name = wpn_addon_grenade_launcher_gp34
